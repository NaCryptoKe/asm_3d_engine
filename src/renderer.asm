; =============================================================================
; renderer.asm — 2.5D raycaster (Phase 2 — stub for now)
; =============================================================================
bits 64

global renderer_frame

section .text

renderer_frame:
    ; TODO Phase 2: raycasting loop
    ret
